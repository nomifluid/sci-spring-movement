import { makeHello } from "shared/module";

print(makeHello("main.server.ts"));

const spring = game.Workspace.FindFirstChild("Spring");
const T = 1;

while (wait(T)) {
	const t = DateTime.now();
	print(t);
}
